import prompt from "prompt-sync"
let ler = prompt();

export function vetor (tab) {
    let numeros = []

for (let i = 1; i <= 10; i++) {
    let item = numeros
    console.log(tab + item + " x " + (i) + " = " + (i * tab))
}
}